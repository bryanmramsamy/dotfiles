# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!


# ---------------| BASICS OF I3 |---------------

# [2019-10-18: UNDERSTOOD] Use of a variable to use the mod-key (Mod4)
set $mod Mod4

# [2020-03-20: UNDERSTOOD] reload the configuration file
bindsym $mod+Shift+c reload
# [2020-03-20: UNDERSTOOD] restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# [2020-03-20: UNDERSTOOD] exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -f 'pango:Ubuntu 10' -m 'DO YOU WANT TO LEAVE i3 ? YOUR Xsession WILL BE ENDED' -b 'Yes, exit i3' 'i3-msg exit'"

# [2019-06-30: OK] Enable NumLock (numlockx)
exec_always --no-startup-id numlockx on

# [2019-06-30: OK] Disable mouse hovering
focus_follows_mouse no

# [2019-05-15: OK] Toggle Touchpad
bindsym XF86TouchpadToggle exec /some/path/toggletouchpad.sh

# [2019-05-15: OK] Touchpad natural scrolling
exec --no-startup-id synclient NaturalScrolling=1 VertScrollDelta=-113


# ---------------| WINDOWS APPEARANCE |---------------

# [2019-10-18: OK] Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Ubuntu Bold, Font Awesome 12

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# [2019-10-18: OK] Get colours from .Xresources and put it into variables
set_from_resource $activ1-bg color4
set_from_resource $activ2-bg color12

set_from_resource $activ1-tx color15
set_from_resource $activ2-tx color7

set_from_resource $inact1-bg color0
set_from_resource $inact2-bg color8

set_from_resource $urgen1-bg color1
set_from_resource $urgen2-bg color9

# [2019-10-18: OK] Custom windows colours
# WINDOW TYPE           TOP-BORDER  WIN-BORDER  TEXT        INDICATOR
client.focused          $activ2-bg  $activ1-bg  $activ1-tx  $activ1-tx
client.unfocused        $inact2-bg  $inact1-bg  $activ2-tx  $activ2-tx
client.focused_inactive $activ2-bg  $activ1-bg  $activ1-tx  $activ1-tx
client.urgent           $urgen2-bg  $urgen1-bg  $activ1-tx  $activ1-tx

# [2019-10-18: DISABLED] Hide windows borders
#hide_edge_borders both

# [2019-10-18: OK] Window's transparancy (compton)
exec --no-startup-id compton

# [2019-10-18: OK] Gaps between windows (i3-gaps)
gaps inner 10  # Gap size between windows
smart_gaps on  # No gap when only one window active
# smart_borders on  # No border when only one window active

# [2019-10-18: DISABLED] Format window title
# for_window [class=".*"] title_format "    %title"

# ---------------| SOFTWARE MANAGEMENT |---------------

# [2020-03-20: UNDERSTOOD] start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# [2019-10-18: DISABLED] start dmenu (a program launcher)
# bindsym $mod+d exec dmenu_run

# [2019-10-18: DISABLED] new i3-dmenu-desktop
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# [2019-10-18: OK] Rofi menu (Documentation: https://www.mankier.com/1/rofi)
# run: shows a list of executables in $PATH and can launch them
bindsym $mod+d exec rofi -modi run -show run
# drun: shows a list of executables in $PATH and can launch them
bindsym $mod+Shift+d exec rofi -modi drun -show drun -show-icons
# window: shows a list of the windows on the current desktop and allows switching between them
bindsym $mod+Tab exec rofi -show window

# [2020-03-11: OK] Network Manager run on startup (Manage WiFi connections)
# exec --no-startup-id nm-applet


# ---------------| WINDOW MANAGEMENT |---------------

# [2020-03-20: UNDERSTOOD] Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# [2020-03-20: UNDERSTOOD] kill focused window
bindsym $mod+Shift+A kill

# [2020-03-20: UNDERSTOOD] change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+m focus right

# [2020-03-20: UNDERSTOOD] alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# [2020-03-20: UNDERSTOOD] move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+M move right

# [2020-03-20: UNDERSTOOD] alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# [2020-03-20: UNDERSTOOD] resize window (you can also use the mouse for that)
bindsym $mod+r mode "RESIZE MODE"

mode "RESIZE MODE" {
    # These bindings trigger as soon as you enter the resize mode

    # Pressing left will shrink the window’s width.
    # Pressing right will grow the window’s width.
    # Pressing up will shrink the window’s height
    # Pressing down will grow the window’s height.
    bindsym j resize shrink width 10 px or 10 ppt
    bindsym k resize grow height 10 px or 10 ppt
    bindsym l resize shrink height 10 px or 10 ppt
    bindsym m resize grow width 10 px or 10 ppt

    # same bindings, but for the arrow keys
    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# [2020-03-20: UNDERSTOOD] split in horizontal orientation
bindsym $mod+h split h

# [2020-03-20: UNDERSTOOD] split in vertical orientation
bindsym $mod+v split v

# [2020-03-20: UNDERSTOOD] enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# [2020-03-20: UNDERSTOOD] change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+z layout tabbed
bindsym $mod+e layout toggle split

# [2020-03-20: UNDERSTOOD] toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# [2020-03-20: UNDERSTOOD] change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# [2020-03-20: UNDERSTOOD] focus the parent container
bindsym $mod+q focus parent

# [2020-03-20: UNDERSTOOD] focus the child container
bindsym $mod+Shift+q focus child


# ---------------| WORKSPACE MANAGEMENT |---------------

# [2019-10-18: OK] Use of variables for workspaces
set $workspace0 "0: Terminal "
set $workspace1 "1: Firefox "
set $workspace2 "2: Browsers  "
set $workspace3 "3: File/Media  "
set $workspace4 "4: Code "
set $workspace5 "5: Communications  "
set $workspace6 "6: Workspace "
set $workspace7 "7: Workspace "
set $workspace8 "8: Workspace "
set $workspace9 "9: Workspace "
set $workspace10 "10: Workspace "
set $workspace11 "11: Workspace "
set $workspace12 "12: Virtual Machines "

# [2019-10-18: OK] Switch to workspace
bindsym $mod+twosuperior workspace $workspace0
bindsym $mod+ampersand workspace $workspace1
bindsym $mod+eacute workspace $workspace2
bindsym $mod+quotedbl workspace $workspace3
bindsym $mod+apostrophe workspace $workspace4
bindsym $mod+parenleft workspace $workspace5
bindsym $mod+section workspace $workspace6
bindsym $mod+egrave workspace $workspace7
bindsym $mod+exclam workspace $workspace8
bindsym $mod+ccedilla workspace $workspace9
bindsym $mod+agrave workspace $workspace10
bindsym $mod+parenright workspace $workspace11
bindsym $mod+minus workspace $workspace12

# [2019-10-18: OK] Move focused container to workspace
bindsym $mod+Shirt+threesuperior move container to workspace $workspace0
bindsym $mod+Shift+1 move container to workspace $workspace1
bindsym $mod+Shift+eacute move container to workspace $workspace2
bindsym $mod+Shift+3 move container to workspace $workspace3
bindsym $mod+Shift+4 move container to workspace $workspace4
bindsym $mod+Shift+5 move container to workspace $workspace5
bindsym $mod+Shift+section move container to workspace $workspace6
bindsym $mod+Shift+egrave move container to workspace $workspace7
bindsym $mod+Shift+8 move container to workspace $workspace8
bindsym $mod+Shift+ccedilla move container to workspace $workspace9
bindsym $mod+Shift+agrave move container to workspace $workspace10
bindsym $mod+Shift+degree move container to workspace $workspace11
bindsym $mod+Shift+underscore move container to workspace $workspace12

# [2019-10-18: OK] Assign specific apps to specific workspaces
assign [class="Firefox"] $workspace1
# for_window [class="firefox-charlie-class"] move to workspace $workspace1
# for_window [class="firefox-delta-class"] move to workspace $workspace1

assign [class="Google-chrome"] $workspace2
assign [class="Opera"] $workspace2

assign [class="Nautilus"] $workspace3
assign [class="Thunar"] $workspace3
assign [class="Nemo"] $workspace3
assign [class="vlc"] $workspace3

assign [class="Code"] $workspace4
assign [class="Sublime_text"] $workspace4
assign [class="StarUML"] $workspace4
assign [class="Eclipse"] $workspace4
assign [class="jetbrains-pycharm-ce"] $workspace4
assign [class="Atom"] $workspace4

assign [class="Thunderbird"] $workspace5
assign [class="whatsdesk"] $workspace5

assign [class="VirtualBox Manager"] $workspace12


# ---------------| DESKTOP PERSONALIZATION |---------------

# [2020-02-08: OK] Desktop wallpaper
exec_always feh --bg-scale /home/bryan/Pictures/Wallpapers/arc_dark_wallpaper.png


# ---------------| I3BAR PERSONALIZATION |---------------

exec_always --no-startup-id "killall polybar; polybar -r default"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
# bar {
# # [2019-10-19: OK] Change font for i3bar
# 	font pango:Ubuntu Condensed Regular, Awesome 10

# # [2019-10-18: OK] Changing the i3bar informations
# #       status_command i3status
# 	status_command i3blocks -c ~/.config/i3/i3blocks.conf

# # [2019-10-18: OK] Place the i3 on the top of the screen
# 	position top

# # [2019-10-18: OK] Customizing the i3 bar with specific colours
# 	colors {
# 		background $inact2-bg
# 		separator #f3f4f5
# 		#			BORDER			BACKGROUND		TEXT
# 		focused_workspace	$inact2-bg		$inact2-bg		$text-color
# 		inactive_workspace	$inactive-bg-color	$inactive-bg-color	$inactive-text-color
# 		urgent_workspace	$urgent-bg-color	$urgent-bg-color	$inactive-text-color
# 	}

# # [2019-06-03: NOT WORKING] Using powerline in the i3 bar
# #	status_command python /home/bryan/.local/lib/python2.7/site-packages/powerline/bindings/i3/powerline-i3.py
# #	font pango:PowerlineFont 12
# }

# [2019-07-28: DOESN'T WORK] Pulse Audio controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound

# [2019-09-22: OK] Pulse Audio controls (Fn-key doesn't work. mod-key used instead)
bindsym $mod+F3 exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
bindsym $mod+F2 exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
bindsym $mod+F1 exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound
bindsym $mod+F4 exec --no-startup-id pactl set-source-mute 1 toggle # mute mic

# SCREEN BRIGHTNESS

# [2019-07-28: DOESN'T WORK - DISABLED] Screen brightness Lenovo shortcut
# bindsym XF86MonBrightnessUp exec xbacklight -inc 10 # increase screen brightness
# bindsym XF86MonBrightnessDown exec xbacklight -dec 10 # decrease screen brightness

# [2019-07-28: OK] Screen brightness Lenovo shortcut (backup fix without using Fn key)
bindsym $mod+F6 exec ~/.config/i3/scripts/acpi_brightness.sh up # increase screen brightness
bindsym $mod+F5 exec ~/.config/i3/scripts/acpi_brightness.sh down # decrease screen brightness


# MEDIAPLAYER

# [2019-07-28: DISABLED] Media player controls
# bindsym XF86AudioPlay exec playerctl play
# bindsym XF86AudioPause exec playerctl pause
# bindsym XF86AudioNext exec playerctl next
# bindsym XF86AudioPrev exec playerctl previous


# LOCKSCREEN

# [2019-06-03: OK] Enable screenlock (i3lock)
# bindsym $mod+Alt_L+l exec i3lock


# AUDIO

# [2019-09-22: OK] Enable sound on i3
exec_always start-pulseaudio-x1


for_window [class="Pavucontrol" instance="pavucontrol"] floating enable

for_window [class="Nm-connection-editor" instance="nm-connection-editor"] floating enable

for_window [class="Wicd-client.py" instance="wicd-client.py"] floating enable

